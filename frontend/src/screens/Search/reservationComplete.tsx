import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ReservationComplete = () => {
    return (
        <View>
            <Text>ReservationComplete</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default ReservationComplete;